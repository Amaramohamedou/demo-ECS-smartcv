# Terraform files
.terraform/

# Terraform state file
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Exclude .terraform directories from recursion by vcs ignore specs
**/.terraform/*

# Ignore .tfvars files that are likely to contain sensitive information
*.tfvars

# Ignore override files as they're usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore CLI tfplan files
*.tfplan

# Ignore terraform.tfvars files, which can contain sensitive variables and
# should not be checked in
terraform.tfvars

# Ignore files used to persist CLI settings across Terraform runs.
.terraform/plugins/**/local_settings.json

# Ignore .terraform directories and terraform.tfstate files generated by the
# Terraform CLI. These should not be checked in to version control.
#
# If you use Jenkins for CI/CD, then this should also be added to your Jenkinsfile:
# `sh 'rm -rf .terraform && terraform init -backend=false && terraform workspace select ' + env.WORKSPACE_NAME`
.terraform/
terraform.tfstate
terraform.tfstate.backup

# Ignore override files as they're usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore CLI tfplan files
*.tfplan

# Ignore terraform.tfvars files, which can contain sensitive variables and
# should not be checked in
terraform.tfvars

# Ignore files used to persist CLI settings across Terraform runs.
.terraform/plugins/**/local_settings.json
